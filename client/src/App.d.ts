import * as React from 'react';
import * as useHistory from 'modules/useHistory';
export const history = useHistory.history;
export const session = string;
export const set_session = React.Dispatch<string>;
export const get_content = (history: history, session: string, set_session: set_session) => JSX.Element;
export const App = JSX.Element;
export default App;;